apiVersion: apps/v1
kind: Deployment
metadata:
  name: inference
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 1
  selector:
    matchLabels:
      app: inference
  template:
    metadata:
      labels:
        app: inference
    spec:
      containers:
      - name: inference
        image: ying703/inference-new:latest #adjust the dockerhub username accordingly
        imagePullPolicy: Always 
        ports:
        - containerPort: 5003
        env:
        - name: MODEL_PATH
          value: "/inference/model/final_model.h5"
        - name: PREPROCESSING_URL
          value: "http://preprocess-svc:5001/preprocess"
        resources:
          requests:
            cpu: 256m
            memory: 512Mi
          limits:
            cpu: 1
            memory: 1Gi
        #for self healing
        livenessProbe:  
          httpGet:
            path: /health
            port: 5003
          initialDelaySeconds: 30
          periodSeconds: 15
        readinessProbe:
          httpGet:
            path: /health
            port: 5003
          initialDelaySeconds: 30
          periodSeconds: 15
        volumeMounts:
        - name: model-storage
          mountPath: /inference/model
      volumes:
      - name: model-storage
        persistentVolumeClaim:
          claimName: model-pvc
